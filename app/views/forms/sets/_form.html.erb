<%= form_for(@forms_set) do |f| %>
  <% if @forms_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forms_set.errors.count, "error") %> prohibited this forms_set from being saved:</h2>

      <ul>
      <% @forms_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
